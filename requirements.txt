# requirements.txt — sv4 (bandeja de revisión)
# Paquete común: pip install -e ../comun (local) / Dockerfile (build)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
jinja2>=3.1
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
sqlalchemy>=2.0.30
psycopg[binary]>=3.1
